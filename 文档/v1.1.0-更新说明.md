新增蓝色置顶边框，跟随窗口移动、缩放和系统圆角设置，最大化时使用直角。最小化隐藏，取消置顶或关闭窗口时清理。

边框适配 PowerToys 的 Direct2D 绘制方式，使用窗口位置事件更新，替换早期的 GDI 区域边框方案。

开机启动改为当前用户启动文件夹快捷方式，并增加托盘加载失败重试。新增项目根目录一键启动脚本，修正构建脚本在 Windows PowerShell 下的中文路径编码。

已验证编译、窗口移动与缩放、最小化与恢复、边框清理和位置事件更新；圆角效果经实际使用确认。

更新方法：退出旧版后替换 WindowPin.exe。由 v1.0.0 更新的用户，请在新版托盘菜单中启用“开机启动”，迁移旧启动项。
